# 세팅 & 사용법 (CareerPilot)

> 목적: 페르소나(CLAUDE.md)·메모리(memory/)·작업 파일(산출물/)을 여러 PC에서 동일하게 사용.

## 구조
```
careerpilot/
├── README.md      # 템플릿 소개 (본인 레포에선 관제탑으로 교체 가능)
├── CLAUDE.md      # 채용 전문가 페르소나 + 이식형 메모리 규칙 (Claude가 자동 로드)
├── .claude/workflows/  # 멀티에이전트 워크플로우 5종(js) — company-research·essay-review·coding-test-debrief·plan-verify·openings-sweep
├── memory/        # 정본 메모리 — MEMORY.md(인덱스) + 도씨에·기록 파일들
│   ├── companies/ #   회사별 폴더: <회사>/status.md = 그 회사의 "현재 상태" 페이지
│   ├── projects/  #   프로젝트·경험 도씨에 (수치·역할의 근거 원장)
│   └── rules/     #   작업 원칙 (실수 교훈을 Why/How to apply 형식으로 박제)
├── 산출물/         # 전 작업 산출물 — 구조·로컬 저지 사용법은 산출물/README.md
│   ├── 지원서/<회사>/  # 회사별 — 제출물/·검증/·자료/ 3분류
│   ├── 코테/           # 전략/·문제/(로컬 저지)·복기/·치트시트/·judge.py
│   └── 캠페인/         # 관제탑.md(데일리 단일 실행 뷰) + 시즌 로드맵·기업 캘린더
└── 세팅_사용법.md   # 이 문서
```
- 히스토리는 memory 루트의 연대기 파일(`job-applications-<시즌>.md`, append-only)에, **회사별 현재 상태는 companies/에** — 역할 분리가 핵심 (구조·형식·운용 원칙 전체는 [memory/README.md](memory/README.md) 참조).

## 새 PC 세팅 (1회, ~5분)
1. Git 설치 + GitHub 로그인(브라우저 인증: 첫 clone/push 때 자동으로 뜸. 본인 GitHub 계정)
2. Claude Code 설치 후 로그인 (기존과 같은 Anthropic 계정)
3. `git clone https://github.com/<본인계정>/<본인레포>.git`
4. 클론된 폴더에서 `claude` 실행 — 끝. 페르소나·메모리·파일이 전부 로드된다.

## 매 세션 루틴 (Claude가 대신 수행 — 잊었으면 "git 루틴 해줘" 한마디)
- 시작: `git pull` → 메모리 로드 확인 ("메모리 상태 확인해줘") → 관제탑(산출물/캠페인/관제탑.md)에서 오늘 할 일 확인
- 종료: 관제탑 최신화 확인 → 커밋 + `git push`

## 주의
- **한 번에 한 PC**에서만 작업. PC를 옮길 땐 이전 PC에서 push 됐는지 먼저 확인.
- 이 레포는 **private** — 개인정보(주소·연락처·성적) 포함. 공개 전환 절대 금지, GitHub 계정 2단계 인증 유지.
- 기존에 다른 곳(로컬 폴더·각 PC의 내장 자동 메모리)에 취준 자료를 두고 있었다면 이 레포로 이전하고 구 경로는 보관용으로 동결 — 정본은 언제나 이 레포.
- 대용량 원자료(발표자료 PDF·소스코드 등)는 레포에 넣지 말 것 — 원본 위치만 memory의 도씨에에 기록해 두면 Claude가 필요할 때 찾는다.
